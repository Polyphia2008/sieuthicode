# Chặn toàn bộ truy cập HTTP trực tiếp vào ảnh chat.
# Ảnh chỉ được stream qua /model/chat/attachment sau khi kiểm tra quyền.
# Không dùng "php_flag engine off" (gây HTTP 500 trên PHP-FPM/FastCGI).
Require all denied

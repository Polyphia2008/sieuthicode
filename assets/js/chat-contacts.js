(function(){'use strict';var list=document.getElementById('chat-contact-list');var search=document.getElementById('chat-contact-search');if(!list)return;var timer;
function esc(s){var d=document.createElement('div');d.textContent=s==null?'':String(s);return d.innerHTML;}
function render(items){if(!items.length){list.innerHTML='<div class="chat-contact-empty">Không tìm thấy quản trị viên.</div>';return;}list.innerHTML=items.map(function(x){return '<a class="chat-contact-item" href="/chat-box/'+encodeURIComponent(x.username)+'"><div class="chat-contact-avatar"><img src="/assets/images/anhdaidien.svg" alt=""><span class="presence-dot '+(x.online?'is-online':'')+'"></span></div><div class="chat-contact-content"><div class="chat-contact-line"><h3>'+esc(x.display_name)+(x.level==='superadmin'?' <i class="fas fa-circle-check contact-super"></i>':' <i class="fas fa-circle-check contact-admin"></i>')+'</h3><time>'+esc(x.time_label)+'</time></div><p>'+esc(x.preview)+'</p></div>'+(x.unread>0?'<span class="contact-unread">'+Math.min(99,x.unread)+'</span>':'')+'</a>';}).join('');}
function load(){fetch('/model/chat/contacts?q='+encodeURIComponent(search?search.value.trim():''),{credentials:'same-origin',headers:{'X-Requested-With':'XMLHttpRequest'}}).then(function(r){return r.json();}).then(function(j){if(j.status==='success')render(j.data.contacts||[]);else list.innerHTML='<div class="chat-contact-empty">'+esc(j.msg)+'</div>';}).catch(function(){list.innerHTML='<div class="chat-contact-empty">Không tải được danh sách.</div>';});}
if(search)search.addEventListener('input',function(){clearTimeout(timer);timer=setTimeout(load,250);});load();setInterval(load,10000);
})();
